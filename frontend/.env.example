VITE_API_BASE=https://breathe-esg-a273.onrender.com

# Copy this file to `.env` locally or set `VITE_API_BASE` on your frontend host
# (Netlify/Vercel/Render). Do NOT commit your actual `.env` file; it's ignored.