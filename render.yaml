# Render manifest to set build and start commands for the web service
services:
  - type: web
    name: breathe-esg-api
    env: docker
    plan: free
    region: oregon
    # Using Docker: Render will build the included Dockerfile and run the container.
    disk: 1024
